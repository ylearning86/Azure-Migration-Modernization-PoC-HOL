# 4.4. 移行アセスメント

Azure Migrate アプライアンスをオンプレ環境にセットアップし、移行元システムの検出・評価を行います。

## 目的

- Azure Migrate アプライアンスを疑似オンプレ環境にデプロイする
- 移行元サーバー（DC01 / DB01 / APP01）を検出する
- Azure VM / Azure SQL / App Service への適合性を評価する
- 評価結果をもとに Spoke1〜4 のどの移行パターンが適切か判断する

## 対象

| サーバー | 主な評価観点 |
| --- | --- |
| `APP01` | Azure VM / App Service / コンテナ化適性 |
| `DB01` | SQL Server → Azure SQL の適性 |
| `DC01` | 基盤系として残置または接続継続の検討 |

## 前提条件

- `rg-onprem` の疑似オンプレ環境がデプロイ済み（[1.2](./1.2-onprem-deploy.md)）
- Parts Unlimited のセットアップ済み（[1.3](./1.3-onprem-parts-unlimited.md)）
- VPN 接続済み（[1.6](./1.6-cloud-vpn-connect.md)）

---

<details>
<summary>Step 1: Azure Migrate プロジェクトの作成</summary>

1. Azure Portal で **Azure Migrate** を検索して開く
2. **[サーバー、データベース、Web アプリ]** を選択
3. **[プロジェクトの作成]** をクリック
4. 以下を入力:

   | 項目 | 値 |
   | --- | --- |
   | サブスクリプション | 使用中のサブスクリプション |
   | リソース グループ | `rg-hub` |
   | プロジェクト名 | `migr-project` |
   | 地域 | `東日本` |

5. **[作成]** をクリック

</details>

---

<details>
<summary>Step 2: アプライアンス VM の作成</summary>

Azure Migrate アプライアンスは、検出対象と同じネットワーク上に配置する必要があります。
オンプレ VNet の `snet-onprem` に VM を作成します。

> **ポイント**: アプライアンスは Azure への接続（検出情報の送信等）が必要なため、`snet-onprem` に NAT Gateway を付与してアウトバウンドインターネットを確保します。

1. Azure Portal で **[仮想マシンの作成]** を開く
2. 以下を入力:

   | 項目 | 値 |
   | --- | --- |
   | リソース グループ | `rg-onprem` |
   | 仮想マシン名 | `vm-onprem-migrate` |
   | リージョン | `東日本` |
   | イメージ | `Windows Server 2022 Datacenter` |
   | サイズ | `Standard_D4s_v3`（4 vCPU / 16 GB 推奨） |
   | 管理者ユーザー名 | `labadmin` |
   | 管理者パスワード | オンプレ環境と同じパスワード |
   | パブリック IP | **なし** |
   | 仮想ネットワーク | `vnet-onprem` |
   | サブネット | `snet-onprem (10.0.1.0/24)` |

3. **[確認と作成]** → **[作成]** をクリック

> **補足**: 実際の移行プロジェクトでは、お客様のオンプレミス環境（Hyper-V / VMware / 物理サーバー）上にアプライアンスを配置します。本 HOL では疑似オンプレ VNet 内の VM で代替します。

</details>

---

<details>
<summary>Step 3: アプライアンスのセットアップ</summary>

### インストーラーの実行

1. Azure Bastion 経由で `vm-onprem-migrate` に RDP 接続する
2. Azure Portal の Azure Migrate プロジェクトで **[検出]** をクリック
3. **[マシンは仮想化されていますか？]** → **[物理またはその他]** を選択
4. **[アプライアンス名]** に `appliance01` と入力（3〜14 文字の制限あり）
5. **[キーの生成]** をクリックしてキーをコピー
6. **[.zip ファイルのダウンロード]** でインストーラーをダウンロード
7. ダウンロードした zip を展開し、`AzureMigrateInstaller.ps1` を管理者 PowerShell で実行:

   ```powershell
   .\AzureMigrateInstaller.ps1
   ```

8. インストーラーの対話プロンプトに以下の順で入力:
   - `3`（Physical or other）
   - `1`（Azure Public）
   - `1`（Public endpoint）
   - `Y`（確認）

9. インストール完了後、Edge ブラウザでアプライアンス構成マネージャーを開く:

   ```powershell
   start microsoft-edge:https://localhost:44368
   ```

   > **注意**: IE は非サポートのため必ず Edge を使用してください。

<details>
<summary>3-1. 前提条件の確認</summary>

1. Configuration Manager が開くと **[Set up prerequisites]** セクションが表示される
2. 自動チェックが実行される（OS バージョン、PowerShell バージョン等）
3. **[Check connectivity to Azure]** が失敗する場合は、`snet-onprem` に NAT Gateway が付与されているか確認
4. すべてのチェックが緑になったら次へ進む

</details>

<details>
<summary>3-2. アプライアンスの登録 (Register with Azure Migrate)</summary>

1. **[Paste key here]** に Step 3-5 でコピーしたキーを貼り付ける
2. **[Verify]** をクリック — キーの検証が行われる
3. 検証成功後、**[Login]** をクリック
4. **デバイスコード認証**のダイアログが表示される:
   - 表示されたコードをコピー
   - **[Copy code & Login]** をクリック
   - 別タブが開くのでコードを貼り付け、Azure アカウントでログイン
5. ログイン完了後、Configuration Manager に戻ると **[Successfully registered]** と表示される

   > **重要**: ログインするアカウントには Azure AD でのアプリ登録権限（Application Developer ロールまたは「Users can register applications = Yes」）が必要です。登録時に SPN（Service Principal）が自動作成されます。

</details>

<details>
<summary>3-3. 資格情報の管理 (Manage credentials and discovery sources)</summary>

1. **[Add credentials]** をクリックし、**[Windows (Domain)]** を選択
2. 以下を入力:

   | 項目 | 値 |
   | --- | --- |
   | フレンドリ名 | `onpremadmin`（ハイフン不可） |
   | ユーザー名 | `lab\labadmin` |
   | パスワード | オンプレ環境の管理者パスワード |

3. **[Save]** をクリック

</details>

</details>

---

<details>
<summary>Step 4: サーバーの検出</summary>

Configuration Manager の **[Manage credentials and discovery sources]** セクションで検出対象を設定します。

<details>
<summary>4-1. 検出ソースの追加</summary>

1. **[Add discovery source]** をクリック
2. **[Add single item]** を選択
3. **[Source type]** で **[Windows Server]** を選択
4. **[Mapped credentials]** で先ほど作成した `onpremadmin` を選択
5. **[IP Address / FQDN]** に以下を 1 行ずつ入力して追加:

   | IP アドレス | サーバー |
   | --- | --- |
   | `10.0.1.4` | DC01（AD DS / DNS） |
   | `10.0.1.5` | DB01（SQL Server） |
   | `10.0.1.6` | APP01（IIS / Web アプリ） |

6. **[Save]** をクリック

</details>

<details>
<summary>4-2. Validation の確認</summary>

1. 各サーバーの **[Status]** 列が **[Validation successful]** になるか確認
2. **[Source validation failed]** が表示される場合:
   - 対象 VM で `winrm quickconfig -force` を管理者 PowerShell で実行
   - アプライアンス VM から `Test-NetConnection -ComputerName <IP> -Port 5985` で接続テスト

</details>

<details>
<summary>4-3. 検出の開始</summary>

1. **[Start discovery]** をクリック
2. 「Discovery has been successfully initiated」と表示される
3. Azure Portal の Azure Migrate ダッシュボードで進捗を確認

Configuration Manager の検出ソース一覧の **Status** は以下のように遷移します:

| Status | 意味 |
| --- | --- |
| Validation successful | WinRM 接続テスト成功（検出開始前） |
| Discovery initiated | 検出が開始された直後 |
| Discovery in progress | サーバー情報を収集中 |
| Discovery Incomplete | 一部完了、残り（SQL / Web アプリ等）は継続中 |
| Discovery completed | すべてのデータ収集が完了 |
| Source discovery failed | 接続エラー等で検出に失敗 |

> **注意**:
>
> - **Discovery initiated** と表示されていれば検出は進行中です
> - 初回は **Source discovery failed** や **Validation Failed** が表示されることがありますが、次の検出サイクル（5〜15 分間隔）で自動的に解消されます
> - サーバーのインベントリ（OS、CPU、メモリ等）は数分で反映されます
> - ソフトウェアインベントリ、SQL Server インスタンス、Web アプリの検出には **数時間〜最大 24 時間** かかります
> - 検出されたサーバーが Azure Portal に反映されるまで最大 15 分程度かかる場合があります

</details>

</details>

---

<details>
<summary>Step 5: 評価の実行</summary>

検出完了後、Azure Portal の Azure Migrate プロジェクトで評価を作成します。

> **注意**: 評価の作成画面は Portal の更新により変更される場合があります。以下は新しい UI に基づく手順です。

<details>
<summary>5-1. Azure VM 評価</summary>

1. **sample-project** → 上部メニューの **[評価の作成]** をクリック
2. **基本情報** タブ:
   - 評価名: `assess-vm`
   - **[Add workloads]** をクリック → 検出済みサーバー一覧から `APP01.lab.local`、`DB01.lab.local` を選択して追加（DC01 は AD 基盤のため移行対象外）
3. **全般** タブ:
   - ターゲットの場所: `Japan East`
4. **詳細** タブ:
   - 必要に応じてストレージの種類等を設定
5. **[評価の確認と作成]** をクリック

</details>

<details>
<summary>5-2. Azure SQL 評価</summary>

1. **[評価の作成]** をクリック
2. **基本情報** タブ:
   - 評価名: `assess-sql`
   - **[Add workloads]** をクリック → `DB01.lab.local` 配下の `MSSQLSERVER` を選択して追加
3. **全般** タブ:
   - ターゲット: `Azure SQL Database`
4. **[評価の確認と作成]** をクリック

</details>

<details>
<summary>5-3. Azure App Service 評価（オプション）</summary>

1. **[評価の作成]** をクリック
2. **基本情報** タブ:
   - 評価名: `assess-appservice`
   - **[Add applications]** をクリック → IIS で検出された Web アプリ（Parts Unlimited）を選択
3. **[評価の確認と作成]** をクリック

> **注意**: Web アプリの検出には数時間かかるため、検出完了後に実施してください。

</details>

</details>

---

<details>
<summary>Step 6: 評価結果の確認</summary>

Azure Portal の Azure Migrate ダッシュボードで、各評価の結果を確認します。

### 確認ポイント

| 評価 | 確認すべき内容 |
| --- | --- |
| Azure VM 評価 | 推奨サイズ、準備状況、月額コスト見積もり |
| Azure SQL 評価 | Azure SQL DB / Managed Instance の推奨、互換性の問題 |
| App Service 評価 | Web アプリの .NET バージョン互換性、移行可能性 |

### 結果を踏まえた移行パターンの選定

| パターン | 判断軸 | 評価結果の見どころ |
| --- | --- | --- |
| Rehost | とにかく早く移したい | VM 評価で推奨サイズとコストを確認 |
| DB PaaS 化 | DB だけ先行してモダナイズしたい | SQL 評価で互換性の問題が少ないか |
| コンテナ化 | DevOps / 移植性 / スケール重視 | App Service 評価で .NET バージョン確認 |
| フル PaaS 化 | 運用負荷を最小化したい | SQL + App Service 評価の両方を確認 |

</details>

---

<details>
<summary>トラブルシューティング</summary>

### AgentAuthenticationSpn エラー

検出開始後に `Source discovery failed` や `An unexpected error occurred on the appliance` と表示される場合、アプライアンスの SPN（Service Principal）設定が不完全な可能性があります。

**確認方法**（アプライアンス VM の管理者 PowerShell で実行）:

```powershell
# Discovery ログでエラーを確認
Get-Content "C:\ProgramData\Microsoft Azure\Logs\Server\Discovery\Service_*.log" -Tail 50
```

`An invalid parameter AgentAuthenticationSpn:TenantId was passed` というエラーが出ている場合、以下で修正できます。

**修正方法**:

アプライアンス VM の管理者 PowerShell で、`serverdiscovery.json` から SPN 情報を `appliance.json` にコピーします:

```powershell
$jsonPath = "C:\ProgramData\Microsoft Azure\Config\appliance.json"
$json = Get-Content $jsonPath -Raw | ConvertFrom-Json
$sdJson = Get-Content "C:\ProgramData\Microsoft Azure\Config\serverdiscovery.json" -Raw | ConvertFrom-Json
$json | Add-Member -NotePropertyName "AgentAuthenticationSpn" -NotePropertyValue $sdJson.AgentAuthenticationSpn -Force
$json | ConvertTo-Json -Depth 10 | Set-Content $jsonPath
Restart-Service -Name "amserverdiscoverysvc" -Force
# 確認
(Get-Content $jsonPath | ConvertFrom-Json).AgentAuthenticationSpn
```

TenantId と ApplicationId が表示されれば修正完了です。

### Azure への接続チェックが失敗する

アプライアンスから Azure への接続には、アウトバウンドインターネット（HTTPS 443）と DNS 名前解決が必要です。

- **NAT Gateway**: `snet-onprem` に NAT Gateway が付与されているか確認
- **DNS**: VNet の DNS 設定がカスタム DNS（例: オンプレ AD）の場合、アプライアンス VM の NIC に Azure DNS (`168.63.129.16`) を直接設定

  ```powershell
  az network nic update -g rg-onprem --name <NIC名> --dns-servers 168.63.129.16 -o none
  ```

### WinRM 接続エラー（Source validation failed）

アプライアンスから検出対象 VM への WinRM（ポート 5985）が通らない場合:

- 各 VM で WinRM を有効化: `winrm quickconfig -force`
- ファイアウォールで別サブネットからのアクセスが制限されている場合: `Set-NetFirewallRule -DisplayGroup "Windows Remote Management" -RemoteAddress Any`

</details>

---

<details>
<summary>クリーンアップ（オプション）</summary>

アセスメント完了後、アプライアンス VM が不要であれば削除できます。

```powershell
az vm delete --resource-group rg-onprem --name vm-onprem-migrate --yes
az disk delete --resource-group rg-onprem --name vm-onprem-migrate_OsDisk_1 --yes --no-wait
az network nic delete --resource-group rg-onprem --name nic-vm-onprem-migrate --no-wait
# NAT Gateway のクリーンアップ
az network vnet subnet update -g rg-onprem --vnet-name vnet-onprem -n snet-onprem --remove natGateway -o none
az network nat gateway delete -g rg-onprem -n natgw-onprem --no-wait
az network public-ip delete -g rg-onprem -n pip-natgw-onprem --no-wait
```

> 移行実行（レプリケーション）フェーズでもアプライアンスを使う場合は削除しないでください。

</details>

---

## 次のステップ

評価結果を確認したら、1 つ以上の移行パターンを実施してください。

- [Step 5: Rehost](./5-cloud-rehost.md) — Rehost (VM → VM)
- [Step 6: DB PaaS 化](./6-cloud-db-paas.md) — DB PaaS 化
- [Step 7: コンテナ化](./7-cloud-containerize.md) — コンテナ化
- [Step 8: フル PaaS 化](./8-cloud-full-paas.md) — フル PaaS 化
